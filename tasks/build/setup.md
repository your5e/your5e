Be able to work on the project, using as similar a setup
to production as possible.

- [ ] django with postgres using Docker
- [ ] `make dev` spins up locally
        - debug mode, logs visible
        - silence postgres checkpoints (just noise)

First migration must include the [custom user model](../users/model.md).
