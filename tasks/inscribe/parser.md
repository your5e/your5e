Create a DSL for encoding system directives into class/item descriptions.

- [ ] define the syntax, how are the directives marked up
- [ ] implement a simple parser to strip the rules out of Markdown
- [ ] process the rules with lark
