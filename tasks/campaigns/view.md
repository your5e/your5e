The campaign page is a placeholder for testing purposes. It needs to become
an actual campaign page.

- [X] seperate settings page to add/remove players, change the join link,
      and update the name and description
        - section-specific navigation with a settings button (like notebooks)
- [X] campaign description should be treated as Markdown
        - displays with the user content font
        - edits with the overtype editor
- [ ] creating a notebook from here should link it to the campaign

Add more views.

- [X] `/campaigns/create` is the form, not on the profile
- [X] `/campaigns/{user}` redirects
- [X] `/campaigns/` lists the campaigns the user owns and is a part of
