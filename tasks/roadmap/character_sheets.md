# Markdown Character Sheets

Encoding the options available in character creation and level up into the
Markdown descriptions themselves, allowing anyone to make their own
classes/subclasses and items.

- [ ] Create the `inscribe` parser                                              @after ../inscribe
- [ ] Annotate the SRD character classes                                        @after
- [ ] Build the character sheet from a choices file                             @after
- [ ] Build the choices file from the character creator                         @after
